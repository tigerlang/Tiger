square(x: int): int {
    return x * x
}

clamp(value: int, min_val: int, max_val: int): int {
    if (value < min_val) {
        return min_val
    }
    if (value > max_val) {
        return max_val
    }
    return value
}